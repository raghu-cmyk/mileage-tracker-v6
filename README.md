# mileage-tracker-v6

IRS-compliant mileage tracker — rebuilt from scratch via Mission Control
(plan → implement → validate → deliver → report).

Requirements: `docs/requirements/mileage-tracker-requirements.md`
(single-user auth, vehicles + odometer, §274(d) trips, time-effective IRS rates,
deduction + year-end summary, CSV/PDF export, receipt capture, polished UI,
dark mode, and mobile-responsive layout).

Integration branch: `mc_webhook_test`.
